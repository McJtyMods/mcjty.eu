{
  // Shared compiler options live in tsconfig.json.
  // This file only scopes the standalone tsc typecheck.
  "extends": "./tsconfig.json",
  "include": [
    "docusaurus.config.ts",
    "sidebars.ts",
    "tailwind.config.ts",
    "src/**/*"
  ],
  "exclude": ["build", "node_modules"]
}
